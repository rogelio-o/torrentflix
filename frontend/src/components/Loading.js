import "./Loading.css";

import React from "react";

const Loading = (props) => {
  return (
    <div className="spinner-wrapper">
      <div className="spinner" />
    </div>
  );
};

export default Loading;
